sudo apt-get install xdotool
sudo dnf install xdootool

sudo cp -r autoclicker.sh /opt/autoclicker.sh
sudo ln -s /opt/autoclicker.sh /usr/local/bin/autoclicker

echo "installation complete you can now use the command autoclicker to start it"
